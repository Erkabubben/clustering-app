image: node:latest

before_script:
  - npm install
  
cache:
  paths:
    - node_modules/
      
# Publish tagged versions to npm
publish:
  stage: deploy
  only:
    - tags
  script:
    - echo '//registry.npmjs.org/:_authToken=${NPM_AUTH}'>.npmrc
    - npm publish
